Project created with React using Typescript
Redux - Toolkit, Thunk, Persist

To run the project:

$ npm install

$ npm start

Note: Inline edit for some fields in Employees Table are not available yet.
